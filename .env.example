# use command below to generate a secret key
## openssl rand -hex 32
DETECTION_API_KEY="your-secret-key"

YOLO_CONF_THRESHOLD=0.35   # 任意
YOLO_IOU_THRESHOLD=0.45    # 任意

# ログ出力関連 (任意)
LOG_LEVEL=INFO
LOG_MAX_BYTES=5242880
LOG_BACKUP_COUNT=5

# 画像リサイズ制限 (任意)
YOLO_MAX_IMAGE_EDGE=2048
YOLO_MAX_IMAGE_PIXELS=4000000
